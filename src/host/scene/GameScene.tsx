/** PLACEHOLDER - replaced by the scene module. Full-viewport 3D canvas for all non-lobby phases. */
export default function GameScene() {
  return <div style={{ position: 'absolute', inset: 0, background: '#050510' }} />
}
